; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env:esp32dev]
platform = espressif32@6.8.1
board = esp32dev
board_upload.flash_size = 4MB
board_build.partitions = partitions.csv
board_build.filesystem = spiffs
framework = espidf
upload_port = COM6
monitor_port = COM6
upload_speed = 921600
monitor_speed = 921600
; One shared firmware supports both physical LCD geometries.
; Edit include/lcd/menu_config.h before compiling: 0 = 16×2, 1 = 20×4.

; TEST ONLY: exposes a simulated physical-feedback input so the mobile power
; controls can be exercised before a real feedback GPIO is installed. It does
; not bypass PIN authentication or any live inverter interlock/state-machine
; checks. Never flash this profile as a production safety build.
[env:esp32dev-ui-mock]
extends = env:esp32dev
build_flags = -DINVERTER_ENABLE_MOCK_PHYSICAL_FEEDBACK=1

; Explicit ADC/LCD verification profiles. The production default remains
; continuous/DMA with the 20x4 LCD; oneshot is the compatibility fallback.
[env:esp32dev-continuous-16x2]
extends = env:esp32dev
build_flags = -DINVERTER_ADC_MODE=0 -DMENU_CONFIG_LCD_20X4=0

[env:esp32dev-oneshot-20x4]
extends = env:esp32dev
build_flags = -DINVERTER_ADC_MODE=1 -DMENU_CONFIG_LCD_20X4=1

[env:esp32dev-oneshot-16x2]
extends = env:esp32dev
build_flags = -DINVERTER_ADC_MODE=1 -DMENU_CONFIG_LCD_20X4=0
